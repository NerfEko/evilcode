 evilcode · v1.0.1
 session: Dracula 🧛
 /model to switch · local:mock · mock-large
 ● ollama-local  ○ ollama-cloud  ○ deepseek  ● mock
 <repo> (main)

 1› fix the clamp

 Fixing the off-by-one.

   ✓ edit testdata/clamp.go · 72 tok (+1 -1)
 ┌─ diff
 │  // makes every scenario's edit fail with "old string not found", and the                                        ╭──────────────────────╮
 │  // goldens then bake in the failure — which is exactly what happened once.                                      │ Context 386/200k     │
 │  func clamp(offset, max int) int {                                                                               │ ▱▱▱▱▱▱▱▱▱▱ 0%        │
 │ -    if offset > max {                                                                                           ╰──────────────────────╯
 │ +    if offset >= max {
 │          return max
 │      }
 │      return offset
 └─ (+1 -1 total)

 That was the clamp bug.
 💡 Ctrl+R searches your prompt history
 2>
   mock-large · 386/200k ctx · dracula
