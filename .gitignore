/evilcode
dist/
probe/frames/
shots/
*.png
!testdata/**/*.png

# Dev scratch notes: kept locally, not shipped with the repo.
# (The dev logs — DEVIATIONS.md, LOOPS.md, planfiles.md — live in docs/.)
lazy.md
new.md
looks.md
lorem.md

# Per-repo evilcode overlay (skills, .evilcode.toml): local config, not shipped.
.evilcode/

# Benchmark rig: local-only, not shipped. Harbor runs, adapters, configs, results.
bench/
